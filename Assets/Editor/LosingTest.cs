using NUnit.Framework;
using UnityEngine;

namespace Thomsen.GuessingGame.Assets.Editor

{
	[TestFixture()]
	public class LosingTest
	{
	    [Test()]
	    public void ShouldMoveToStateStartOnLoss()
	    {//cant use this test - need an integration test
/*
                        Losing losing = new Losing();
                        Guesser guesser = new Guesser();

                        Assert.IsInstanceOf<StartState>(losing.HandleInput(guesser, KeyCode.Return));*/
	    }
	}
}
